# Credentials — never commit
.env

# ChromaDB local vector store — committed for Railway deployment
# chroma_db/

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
venv/
env/
.venv/

# Audit logs — generated at runtime
audit_log.json

# IBM program materials — do not publish
assets/IBM*

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Jupyter (if notebooks added later)
.ipynb_checkpoints/
*.ipynb
# Data files — included for Railway deployment
# Large CSV is 58 MB (under GitHub's 100 MB hard limit)
!data/
!data/svi_2022_us_tract.csv
!data/empower_oh_ok.json
!data/tri_facilities_oh_ok.json
!data/policy_docs/
!data/policy_docs/*.txt

# Draft assessment variants (do not track)
agents/assessments/
