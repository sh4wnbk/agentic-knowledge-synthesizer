.ibm_survival_gap/
.git/
.env
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
# This repo's venv is named for the project, not .venv, so the generic
# patterns above miss it. Without this line COPY . . bakes 1.8 GB of local
# site-packages into the image.
aegis/
.DS_Store
orchestrate/logs/
orchestrate/generated_audio/
audit_log.json
.pytest_cache/
